export default function home() {
    return(
        <h1>Home Page</h1>
        // TODO: Display non User related data if nobody is signed in
        // TODO: Display user related data if signed in
    );
}